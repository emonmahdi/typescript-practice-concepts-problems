
type Person = {
    name: string,
    age: number,
    email?: string,
    contactNumber: number
}

// pick
type nameValue = Pick<Person, "name" | "age"> 

// omit
type removeValue = Omit<Person, "name" | 'contactNumber'>

// Required
type requiredValue = Required<Person>

// ReadOnly
type readOnlyValue = Readonly<Person>

const user: readOnlyValue = {
    name: 'emon',
    age: 12,
    email: '@gmail.com',
    contactNumber: 2332
}

// user.name = 'Habib'  not change because it's readOnly


// Record 
type recordValue = Record<string, unknown>

const myObj: recordValue = {
    a: 'as',
    b: 'bc',
    c: 'bd',
    d: 'ed',
    e: 32
}


/* 

চলো আমরা আজ শিখি 👉 **TypeScript Utility Types** — যেগুলো অনেক শক্তিশালী বিল্ট-ইন হেল্পার টাইপ, যেগুলো তোমার কোডকে ছোট, পরিষ্কার, এবং পুনর্ব্যবহারযোগ্য করে তোলে। ❤️
সবগুলো ইউটিলিটি টাইপ আমরা **সহজ বাংলা ব্যাখ্যা + কোড উদাহরণ** সহ দেখব 👇

---

## 🔹 ১. `Partial<T>`

**কাজ:**
একটা টাইপের সব প্রপার্টিকে *optional* (ঐচ্ছিক) করে দেয়।

**Example:**

```ts
interface User {
  name: string;
  age: number;
  email: string;
}

const updateUser = (data: Partial<User>) => {
  console.log(data);
};

updateUser({ name: "Emon" }); // ঠিক আছে
```

**বাংলা ব্যাখ্যা:**
এখানে `Partial<User>` মানে `User` এর সব প্রপার্টি এখন চাইলে বাদ দিতে পারো।

---

## 🔹 ২. `Required<T>`

**কাজ:**
সব প্রপার্টিকে *required* (অবশ্যই দিতে হবে) করে দেয়।

**Example:**

```ts
interface User {
  name?: string;
  age?: number;
}

const createUser = (data: Required<User>) => {
  console.log(data);
};

createUser({ name: "Emon", age: 25 }); // ✅
createUser({ name: "Emon" }); // ❌ age বাদ দিলে error
```

**বাংলা ব্যাখ্যা:**
`Required` সব প্রপার্টিকে আবশ্যক করে তোলে, কোনোটাই বাদ দেওয়া যাবে না।

---

## 🔹 ৩. `Readonly<T>`

**কাজ:**
সব প্রপার্টিকে শুধু *read-only* করে দেয় — মানে একবার মান সেট হলে পরে পরিবর্তন করা যাবে না।

**Example:**

```ts
interface User {
  name: string;
  age: number;
}

const user: Readonly<User> = { name: "Emon", age: 24 };
user.age = 25; // ❌ error: read-only property
```

**বাংলা ব্যাখ্যা:**
`Readonly` মানে — শুধু পড়া যাবে, লেখা যাবে না।

---

## 🔹 ৪. `Pick<T, K>`

**কাজ:**
একটা টাইপ থেকে নির্দিষ্ট কিছু প্রপার্টি *তুলে নেওয়া* যায়।

**Example:**

```ts
interface User {
  id: number;
  name: string;
  age: number;
  email: string;
}

type BasicUser = Pick<User, "id" | "name">;

const user: BasicUser = { id: 1, name: "Emon" };
```

**বাংলা ব্যাখ্যা:**
`Pick` ব্যবহার করে বড় টাইপ থেকে নির্দিষ্ট কিছু প্রপার্টি আলাদা করে ছোট টাইপ বানানো যায়।

---

## 🔹 ৫. `Omit<T, K>`

**কাজ:**
একটা টাইপ থেকে নির্দিষ্ট কিছু প্রপার্টি *বাদ দেওয়া* যায়।

**Example:**

```ts
interface User {
  id: number;
  name: string;
  age: number;
  email: string;
}

type UserWithoutEmail = Omit<User, "email">;

const user: UserWithoutEmail = { id: 1, name: "Emon", age: 25 };
```

**বাংলা ব্যাখ্যা:**
`Omit` মানে — কিছু প্রপার্টি বাদ দিয়ে নতুন টাইপ তৈরি করা।

---

## 🔹 ৬. `Record<K, T>`

**কাজ:**
একটা অবজেক্ট টাইপ তৈরি করে যেখানে প্রতিটা key-এর টাইপ এবং value-এর টাইপ নির্ধারিত থাকে।

**Example:**

```ts
type Roles = "admin" | "user" | "guest";
type RolePermissions = Record<Roles, string>;

const permissions: RolePermissions = {
  admin: "all-access",
  user: "limited-access",
  guest: "read-only",
};
```

**বাংলা ব্যাখ্যা:**
`Record` এমন টাইপ তৈরি করে যেখানে key-value দুটোই নির্দিষ্টভাবে সংজ্ঞায়িত।

---

## 🔹 ৭. `Exclude<T, U>`

**কাজ:**
একটা ইউনিয়ন টাইপ থেকে নির্দিষ্ট টাইপ বাদ দেয়।

**Example:**

```ts
type Roles = "admin" | "user" | "guest";
type NonGuest = Exclude<Roles, "guest">; // "admin" | "user"
```

**বাংলা ব্যাখ্যা:**
`Exclude` মানে — যে টাইপ বাদ দিতে চাও সেটা বাদ দিয়ে বাকি টাইপ রেখে দেওয়া।

---

## 🔹 ৮. `Extract<T, U>`

**কাজ:**
একটা ইউনিয়ন টাইপ থেকে নির্দিষ্ট টাইপ *তুলে নেয়*।

**Example:**

```ts
type Roles = "admin" | "user" | "guest";
type OnlyGuest = Extract<Roles, "guest">; // "guest"
```

**বাংলা ব্যাখ্যা:**
`Extract` মানে — যেটা মিলছে সেটা শুধু রেখে বাকি বাদ দেওয়া।

---

## 🔹 ৯. `NonNullable<T>`

**কাজ:**
`null` এবং `undefined` বাদ দেয়।

**Example:**

```ts
type User = string | null | undefined;
type ValidUser = NonNullable<User>; // string
```

**বাংলা ব্যাখ্যা:**
`NonNullable` মানে — এমন টাইপ বানানো যেখানে `null` বা `undefined` থাকবে না।

---

## 🔹 ১০. `ReturnType<T>`

**কাজ:**
একটা ফাংশনের *return টাইপ* বের করে দেয়।

**Example:**

```ts
function getUser() {
  return { name: "Emon", age: 25 };
}

type UserType = ReturnType<typeof getUser>; // { name: string; age: number }
```

**বাংলা ব্যাখ্যা:**
`ReturnType` ফাংশনের রিটার্নের টাইপ বুঝে সেটাকে আলাদা করে নেয়।

---

## 🔹 ১১. `Parameters<T>`

**কাজ:**
একটা ফাংশনের *parameter টাইপগুলো* বের করে দেয়।

**Example:**

```ts
function setUser(name: string, age: number) {}

type Params = Parameters<typeof setUser>; // [string, number]
```

**বাংলা ব্যাখ্যা:**
`Parameters` ফাংশনের প্যারামিটারগুলোর টাইপ একটি অ্যারের আকারে দেয়।

---

## 🔹 ১২. `ConstructorParameters<T>`

**কাজ:**
একটা ক্লাসের কনস্ট্রাক্টরের প্যারামিটার টাইপ বের করে।

**Example:**

```ts
class Person {
  constructor(public name: string, public age: number) {}
}

type PersonParams = ConstructorParameters<typeof Person>; // [string, number]
```

**বাংলা ব্যাখ্যা:**
`ConstructorParameters` ক্লাসের কনস্ট্রাক্টরের ইনপুট টাইপ দেয়।

---

## 🔹 ১৩. `InstanceType<T>`

**কাজ:**
একটা ক্লাসের ইনস্ট্যান্স টাইপ বের করে।

**Example:**

```ts
class Car {
  brand = "Toyota";
}

type CarInstance = InstanceType<typeof Car>;
const car: CarInstance = new Car();
```

**বাংলা ব্যাখ্যা:**
`InstanceType` ক্লাস থেকে তৈরি অবজেক্টের টাইপ দেয়।

---

## 🔹 ১৪. `Awaited<T>`

**কাজ:**
একটা `Promise` এর রিটার্ন টাইপ বের করে দেয়।

**Example:**

```ts
type Response = Promise<string>;
type ResolvedType = Awaited<Response>; // string
```

**বাংলা ব্যাখ্যা:**
`Awaited` মানে — `Promise` resolve হওয়ার পর ভেতরের টাইপটা বের করা।

---

## 🧠 সারসংক্ষেপ (Quick Summary Table)

| Utility Type               | কাজ করে যা                           | উদাহরণ                                |           |
| -------------------------- | ------------------------------------ | ------------------------------------- | --------- |
| `Partial<T>`               | সব প্রপার্টি optional করে            | `Partial<User>`                       |           |
| `Required<T>`              | সব প্রপার্টি required করে            | `Required<User>`                      |           |
| `Readonly<T>`              | সব প্রপার্টি read-only করে           | `Readonly<User>`                      |           |
| `Pick<T,K>`                | কিছু প্রপার্টি বেছে নেয়              | `Pick<User, "name">`                  |           |
| `Omit<T,K>`                | কিছু প্রপার্টি বাদ দেয়               | `Omit<User, "email">`                 |           |
| `Record<K,T>`              | নির্দিষ্ট key-value টাইপ তৈরি করে    | `Record<"admin","string">`            |           |
| `Exclude<T,U>`             | নির্দিষ্ট টাইপ বাদ দেয়               | `Exclude<"a"                          | "b","b">` |
| `Extract<T,U>`             | নির্দিষ্ট টাইপ নেয়                   | `Extract<"a"                          | "b","b">` |
| `NonNullable<T>`           | null/undefined বাদ দেয়               | `NonNullable<string                   | null>`    |
| `ReturnType<T>`            | ফাংশনের রিটার্ন টাইপ নেয়             | `ReturnType<typeof fn>`               |           |
| `Parameters<T>`            | ফাংশনের প্যারামিটার টাইপ নেয়         | `Parameters<typeof fn>`               |           |
| `ConstructorParameters<T>` | ক্লাসের কনস্ট্রাক্টর প্যারামিটার নেয় | `ConstructorParameters<typeof Class>` |           |
| `InstanceType<T>`          | ক্লাস ইনস্ট্যান্স টাইপ নেয়           | `InstanceType<typeof Class>`          |           |
| `Awaited<T>`               | Promise resolve টাইপ নেয়             | `Awaited<Promise<T>>`                    |           |

---

চাও কি আমি এখন প্রতিটি **Utility Type-এর ছোট ছোট প্র্যাকটিস টাস্ক** ও **ইন্টারভিউ প্রশ্ন** যোগ করে দিই?
(যেমন `Partial`, `Pick`, `Record` নিয়ে ১-২টি করে প্র্যাকটিস ও প্রশ্ন)   


*/